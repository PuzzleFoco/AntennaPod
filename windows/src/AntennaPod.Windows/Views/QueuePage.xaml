<Page
    x:Class="AntennaPod.Windows.Views.QueuePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Queue" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
        </Grid>

        <ListView x:Name="QueueList" Grid.Row="1" SelectionMode="None" CanReorderItems="True"
                  AllowDrop="True" IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,12" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <FontIcon Grid.Column="0" Glyph="&#xE700;" FontSize="12" Opacity="0.5"
                                 VerticalAlignment="Center" />

                        <Border Grid.Column="1" Width="40" Height="40" CornerRadius="4"
                                Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                            <FontIcon Glyph="&#xE768;" FontSize="14"
                                     HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}"
                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                       TextTrimming="CharacterEllipsis" MaxLines="1" />
                            <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                <Run Text="{Binding Duration}" /><Run Text="s" />
                            </TextBlock>
                        </StackPanel>

                        <Button Grid.Column="3" Style="{StaticResource SubtleButtonStyle}"
                                VerticalAlignment="Center" ToolTipService.ToolTip="Remove from queue">
                            <FontIcon Glyph="&#xE711;" FontSize="14" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Collapsed">
            <FontIcon Glyph="&#xE8FD;" FontSize="48" Opacity="0.5" HorizontalAlignment="Center" Margin="0,0,0,16" />
            <TextBlock Text="Queue is empty" Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center" Opacity="0.7" />
            <TextBlock Text="Add episodes to your queue to listen to them later"
                       Style="{StaticResource BodyTextBlockStyle}" HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="False"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Page>
