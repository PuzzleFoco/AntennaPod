<Page
    x:Class="AntennaPod.Windows.Views.EpisodesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AntennaPod.Windows.Views"
    NavigationCacheMode="Disabled">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton" Grid.Column="0" Style="{StaticResource SubtleButtonStyle}"
                    Click="BackButton_Click" Visibility="Collapsed" Margin="0,0,16,0">
                <FontIcon Glyph="&#xE72B;" FontSize="14" />
            </Button>
            <TextBlock x:Name="PageTitle" Grid.Column="1" Text="Episodes"
                       Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <ComboBox x:Name="FilterCombo" SelectionChanged="FilterCombo_SelectionChanged" Width="150">
                    <ComboBoxItem Content="All Episodes" IsSelected="True" />
                    <ComboBoxItem Content="Unplayed" />
                    <ComboBoxItem Content="In Progress" />
                    <ComboBoxItem Content="Downloaded" />
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Episodes list -->
        <ListView x:Name="EpisodeList" Grid.Row="1" SelectionMode="None"
                  IsItemClickEnabled="True" ItemClick="EpisodeList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,12" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Play state indicator -->
                        <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20"
                                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE768;" FontSize="14"
                                     HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <!-- Episode details -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}"
                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                       TextTrimming="CharacterEllipsis" MaxLines="2" />
                            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,4,0,0">
                                <TextBlock Text="{Binding PublishedDate}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                    <Run Text="{Binding Duration}" />
                                    <Run Text="s" />
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <!-- Download button -->
                        <Button Grid.Column="2" Style="{StaticResource SubtleButtonStyle}"
                                VerticalAlignment="Center" ToolTipService.ToolTip="Download">
                            <FontIcon Glyph="&#xE896;" FontSize="14" />
                        </Button>

                        <!-- More options -->
                        <Button Grid.Column="3" Style="{StaticResource SubtleButtonStyle}"
                                VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE712;" FontSize="14" />
                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Add to Queue">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE8FD;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem Text="Mark as Played">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE73E;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem Text="Share">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE72D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Loading -->
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="False"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Empty state -->
        <StackPanel x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Collapsed">
            <FontIcon Glyph="&#xEA69;" FontSize="48" Opacity="0.5" HorizontalAlignment="Center" Margin="0,0,0,16" />
            <TextBlock Text="No episodes" Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center" Opacity="0.7" />
        </StackPanel>
    </Grid>
</Page>
